__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
build/
dist/
.venv/
.worktrees/
# uv is the documented local workflow, but this is a setuptools-published
# library whose CI installs with pip, so the resolved lockfile is local state.
uv.lock
